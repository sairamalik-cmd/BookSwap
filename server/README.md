# BookSwap - Server

Simple Express + MongoDB backend scaffold for BookSwap.

Quick start

1. Copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`.
2. Install deps:

```bash
npm install
```

3. Run in dev:

```bash
npm run dev
```
